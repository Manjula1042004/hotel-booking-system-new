# =============================================
# HOTEL BOOKING SYSTEM - PRODUCTION PROFILE
# =============================================

# PostgreSQL Configuration for Render
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://dpg-d3m9ejpr0fns73ehpjug-a:5432/hotel_booking_db_0ore
spring.datasource.username=hotel_booking_db_0ore_user
spring.datasource.password=oZ7HVuom6sf9AUfbRonKbyWK9Cl5YruZ

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# PostgreSQL Dialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Remove MySQL backticks for PostgreSQL
spring.jpa.properties.hibernate.globally_quoted_identifiers=false
spring.jpa.properties.hibernate.globally_quoted_identifiers_skip_column_definitions=true
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# Render Port Binding
server.port=${PORT:10000}
server.address=0.0.0.0

# Thymeleaf Configuration (Production)
spring.thymeleaf.cache=true

# Logging (Production - Less Verbose)
logging.level.com.hotel.booking_system=INFO
logging.level.org.springframework.security=WARN
logging.level.org.hibernate.SQL=WARN

# Static Resources
spring.web.resources.add-mappings=true
spring.web.resources.static-locations=classpath:/static/